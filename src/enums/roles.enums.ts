export enum RolesEnum {
    ADMIN = "ADMIN",
    CONSUMER = "CONSUMER",
    VIGIL = "VIGIL",
    GUEST = "GUEST",
    
    // TODO other roles
}

export enum AccessLevelsEnum {
    BASE = "BASE",
    PREMIUM = "PREMIUM",
    GOD = "GOD",
    // TODO other levels
}

export enum UserStatusEnum {
    INACTIVE = "inactive",
    PENDING = "pending",
    ACTIVE = "active",
    SUSPENDED = "suspended",
    DELETED = "deleted",
}